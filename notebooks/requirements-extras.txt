# Optional extras for full deck coverage
# Install with: pip install -r notebooks/requirements-extras.txt

# Networks
osmnx
networkx

# Notebooks
jupyter
jupyterlab
ipykernel

# Spatial indexing
rtree

# Additional utilities referenced or useful
rasterstats
contextily
matplotlib

# Point patterns (deck 05 Part 2, notebook 04b: quadrats, KDE, G/F/K envelopes)
pointpats

# Point clouds (optional, mentioned conceptually)
laspy
open3d

# Machine learning (decks 06 ML integration + 09 capstone notebooks)
scikit-learn
gstools          # variograms & kriging (RFsp vs kriging, notebook 09_B)
torch            # GNNs + location encoders (notebooks 10_C, 11_D)
torch_geometric  # GCN / Planetoid Cora loader; install may need CPU wheel index:
                 #   pip install torch_geometric  (see pyg.org/get-started for pinned wheels)
# geoclip        # optional: pretrained GeoCLIP location encoder (pulls torch)
